; Private: the generator is an implementation detail of the `sqlml` binary and
; the test suite. (package) keeps it buildable under `dune build -p`.

(library
 (name sqlml_gen)
 (package sqlml-postgresql)
 (libraries unix otoml pq))
